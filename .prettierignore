node_modules
**/dist
**/.next
**/*.tsbuildinfo
pnpm-lock.yaml
# Generated from tokens.ts — never hand-format.
packages/tokens/src/tokens.css
# Legacy static site (huge, hand-authored HTML) — out of scope.
frontend
index.html
*.html
# Backend is CJS with deliberate manual alignment (aligned `=` requires,
# aligned route mounts). Prettier would flatten it. Same scoping as ESLint;
# format it separately if/when that style is retired.
backend
