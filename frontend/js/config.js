// ── API configuration ────────────────────────────────────────────────────────
// Empty string = relative URL (same host/port as the page).
// Works in dev (localhost:3000), staging, and production without any change.
// Only update this if the API is hosted on a DIFFERENT domain than the frontend.
const CONFIG = {
  API_BASE: ''
};
